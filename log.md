4.0.5 更新内容
1. 修复“AI 明明画了，界面上却不显示”的主要成因：XML 被截断/没闭合时以前会整块丢掉，现在会自动补全闭合标签再渲染
2. 新增空白卡片诊断：卡片里没有可见元素、尺寸为 0、内容全在未显示的弹层里时，直接提示原因，不再默默空白
3. 第一个场景是空的、内容在后面场景里时，自动切到有内容的场景
4. 关闭“允许 AI 使用 XML 画布”时会明确提示“AI 画了 N 张卡片，但被设置挡住了”
5. 每次渲染都会写日志（场景数/元素数/尺寸），方便排查
