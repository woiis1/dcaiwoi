4.0.1 更新内容
1. 默认更新源改为 GitHub 官方接口（实时、无 CDN 缓存），并按顺序自动尝试 jsDelivr、raw 直链与备用镜像
2. 下载支持多地址自动切换：某个地址连不上会立刻换下一个，不用手动重试
3. 版本号、大小、MD5、更新日志全部由一键发版脚本自动写入，发布更省心
4. 修复部分网络下 raw.githubusercontent 被 DNS 污染导致检查不到更新的问题
5. 发版脚本改为多线路推送（HTTPS / SSH 443 / HTTP1.1 轮换），网络抖动时会一直重试到成功
